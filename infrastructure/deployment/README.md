# Deployment

The deployment boundary remains a modular monolith. Workers use the same repository and may use the API image with different startup commands.

No Kubernetes is required by the foundation handoff.
